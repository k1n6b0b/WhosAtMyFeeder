# macOS metadata
._*
.DS_Store

# Local dev staging dir
_frigate_overrides/

# Config (contains credentials — never commit)
config/*
!config/config.yml.example

# Data files (not for version control)
data/
*.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.coverage
htmlcov/

# Local dev compose override
docker-compose.dev.yml

# MCP config (contains private token)
.mcp.json
